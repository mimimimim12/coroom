# coroom — 회의실 예약 시스템 기획서 (PRD)

## 1. 개요

**coroom**은 사내 회의실 6개의 예약 현황을 한눈에 보여주고, 비어 있는 시간대를 클릭하여 바로 예약할 수 있는 웹사이트입니다. 별도의 문의나 담당자 확인 없이, 누구나 화면에서 빈 시간을 확인하고 즉시 예약을 완료하는 것을 목표로 합니다.

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | coroom |
| 한 줄 설명 | 회의실 예약 현황을 한눈에 보고, 빈 시간을 눌러 바로 예약하는 웹사이트 |
| 대상 사용자 | 사내 임직원 (예약자) |
| 플랫폼 | 웹 (반응형, 데스크톱 우선) |
| 데이터베이스 | Supabase (PostgreSQL) |
| 기준일 | 2026-07-14 |

---

## 2. 목표 및 핵심 가치

### 2.1 목표
- 6개 회의실의 예약 현황을 **하나의 타임테이블**에서 직관적으로 파악
- 빈 시간대를 **클릭 한 번**으로 예약 시작 → 최소 입력으로 완료
- 예약 중복(더블 부킹) 방지

### 2.2 핵심 가치
- **한눈에 보기**: 날짜별 · 회의실별 예약 상태를 색상과 블록으로 시각화
- **즉시 예약**: 빈 슬롯 클릭 → 예약 폼 → 확정까지 3스텝 이내
- **신뢰성**: 확정/취소 상태 관리, 시간 겹침 검증

---

## 3. 사용자 시나리오

1. 사용자가 coroom에 접속하면 오늘 날짜 기준 6개 회의실의 타임테이블이 보인다.
2. 원하는 날짜를 선택한다. (기본값: 오늘)
3. 원하는 회의실과 시간대의 **빈 칸(빈 슬롯)** 을 클릭한다.
4. 예약 폼이 열리고, 이미 회의실/날짜/시작시간이 채워져 있다.
5. 예약자 이름, 부서, 회의제목, 종료시간을 입력하고 "예약하기"를 누른다.
6. 중복 여부 검증 후 예약이 생성되고, 타임테이블에 즉시 예약 블록이 표시된다.
7. 필요 시 자신의 예약을 클릭하여 상세 확인 및 취소할 수 있다.

---

## 4. 데이터 모델 (Supabase)

첨부된 `meetingroom_reservations.xlsx`의 **회의실목록**, **예약현황** 두 시트를 기반으로 설계합니다.

### 4.1 `rooms` (회의실)

| 컬럼 | 타입 | 설명 | 예시 |
| --- | --- | --- | --- |
| `id` | `int` (PK) | 회의실번호 (1~6) | 3 |
| `name` | `text` | 회의실명 | 3번 회의실 (중회의실 A) |
| `capacity` | `int` | 수용인원 | 8 |
| `floor` | `text` | 층 | 3층 |
| `equipment` | `text` | 보유장비 | 빔프로젝터, 화상회의 카메라 |
| `note` | `text` (nullable) | 비고 | 임원 보고용 우선 배정 |

**초기 데이터 (seed)**

| id | name | capacity | floor | equipment | note |
| --- | --- | --- | --- | --- | --- |
| 1 | 1번 회의실 (소회의실 A) | 4 | 3층 | TV, 화이트보드 | — |
| 2 | 2번 회의실 (소회의실 B) | 4 | 3층 | TV, 화이트보드 | — |
| 3 | 3번 회의실 (중회의실 A) | 8 | 3층 | 빔프로젝터, 화상회의 카메라 | — |
| 4 | 4번 회의실 (중회의실 B) | 8 | 4층 | 빔프로젝터, 화이트보드 | — |
| 5 | 5번 회의실 (대회의실) | 16 | 4층 | 빔프로젝터, 화상회의 카메라, 음향장비 | 임원 보고용 우선 배정 |
| 6 | 6번 회의실 (스튜디오) | 6 | 4층 | 방음시설, 녹화장비 | 면접/촬영 겸용 |

### 4.2 `reservations` (예약)

| 컬럼 | 타입 | 설명 | 예시 |
| --- | --- | --- | --- |
| `id` | `text` (PK) | 예약번호 | B2026071 |
| `room_id` | `int` (FK → rooms.id) | 회의실번호 | 3 |
| `reserver` | `text` | 예약자 | 박도윤 |
| `department` | `text` | 부서 | 마케팅팀 |
| `title` | `text` | 회의제목 | 주간 팀 회의 |
| `date` | `date` | 예약일자 | 2026-07-13 |
| `start_time` | `time` | 시작시간 | 16:00 |
| `end_time` | `time` | 종료시간 | 18:00 |
| `status` | `text` | 상태 (`확정` / `취소`) | 확정 |
| `created_at` | `timestamptz` | 생성일시 (자동) | — |

> **예약번호 규칙**: `B` + 연도(4자리) + 순번(3자리), 예) `B2026096`. 신규 생성 시 시퀀스로 자동 부여.

### 4.3 제약 조건 및 인덱스
- `reservations.room_id`는 `rooms.id`를 참조하는 외래키.
- **중복 예약 방지**: 동일 `room_id` + `date`에서 `status = '확정'`인 예약끼리 `[start_time, end_time)` 시간 구간이 겹치면 안 됨.
  - 애플리케이션 레벨 검증 + (권장) DB 레벨 `EXCLUDE` 제약 또는 트리거로 이중 방어.
- 조회 성능을 위해 `(room_id, date)` 복합 인덱스 생성.
- **RLS(Row Level Security)**: 초기 버전은 사내 사용을 전제로 익명 읽기/쓰기 허용, 추후 인증 도입 시 정책 강화.

---

## 5. 기능 요구사항

### 5.1 예약 현황 조회 (핵심)
- **날짜 선택기**: 기본값 오늘. 이전/다음 날 이동, 달력에서 날짜 선택.
- **타임테이블 뷰**: 세로축 = 시간(예: 09:00~19:00, 30분 또는 1시간 단위), 가로축 = 6개 회의실.
  - 예약된 시간대는 **예약 블록**으로 표시 (회의제목 · 예약자 요약).
  - 빈 시간대는 **클릭 가능한 빈 슬롯**으로 표시.
  - `취소` 상태 예약은 타임테이블에서 점유하지 않음(빈 슬롯 취급).
- 회의실별 정보(수용인원 · 층 · 장비)를 헤더 또는 툴팁으로 노출.

### 5.2 예약 생성 (핵심)
- 빈 슬롯 클릭 시 예약 폼 모달 오픈. **회의실 · 날짜 · 시작시간**은 자동 채움.
- 입력 항목: 예약자, 부서, 회의제목, 종료시간(기본 시작+1시간).
- 제출 시 검증:
  - 필수값 확인.
  - 종료시간 > 시작시간.
  - 동일 회의실·날짜에 확정 예약과 시간 겹침 없음.
- 성공 시 예약번호 자동 부여, 상태 `확정`, 타임테이블 즉시 갱신.

### 5.3 예약 상세 / 취소
- 예약 블록 클릭 시 상세 정보 표시(예약번호, 예약자, 부서, 제목, 시간).
- 취소 시 `status = '취소'`로 변경(soft delete). 취소된 슬롯은 다시 예약 가능.

### 5.4 (선택) 부가 기능 — 향후 확장
- 부서/예약자별 필터, 검색.
- 주간(week) 뷰.
- 사용자 인증 및 본인 예약만 취소 가능.
- 예약 알림(이메일 등).

---

## 6. 화면 구성 (UI)

### 6.1 메인 화면
```
┌─────────────────────────────────────────────────────────┐
│  coroom                                  [◀ 2026-07-14 ▶]│
├──────┬──────┬──────┬──────┬──────┬──────┬────────────────┤
│ 시간 │ 1번  │ 2번  │ 3번  │ 4번  │ 5번  │ 6번            │
├──────┼──────┼──────┼──────┼──────┼──────┼────────────────┤
│09:00 │      │      │[화상]│      │      │                │
│10:00 │      │      │ 미팅 │      │      │                │
│11:00 │      │      │      │      │      │  ← 빈 슬롯 클릭 │
│ ...  │      │      │      │      │      │                │
└──────┴──────┴──────┴──────┴──────┴──────┴────────────────┘
```
- 상단: 로고, 날짜 네비게이션.
- 본문: 회의실 × 시간 그리드. 예약 블록은 색상, 빈 슬롯은 호버 시 강조.

### 6.2 예약 폼 모달
- 회의실(고정) / 날짜(고정) / 시작시간(고정, 수정 가능)
- 예약자 · 부서 · 회의제목 · 종료시간 입력
- [예약하기] / [닫기]

---

## 7. 기술 스택

| 구분 | 기술 |
| --- | --- |
| 프론트엔드 | HTML / CSS / JavaScript (SPA) + supabase-js |
| 백엔드/DB | Supabase (PostgreSQL, Auth, Realtime) |
| 실시간 갱신 | (선택) Supabase Realtime 구독으로 예약 변경 즉시 반영 |
| 배포 | 정적 호스팅 (예: Vercel) |

---

## 8. 비기능 요구사항
- **성능**: 날짜 전환 및 예약 반영 1초 이내.
- **동시성**: 동시 예약 시 서버(DB) 제약으로 더블 부킹 방지.
- **반응형**: 데스크톱 우선, 태블릿 대응.
- **접근성**: 색상만이 아닌 텍스트로도 예약/빈 상태 구분.

---

## 9. 마일스톤 (제안)

| 단계 | 내용 |
| --- | --- |
| 1 | Supabase 프로젝트 생성, `rooms`/`reservations` 테이블 및 seed 데이터 구축 |
| 2 | 타임테이블 조회 화면 (날짜 선택 + 6개 회의실 그리드) |
| 3 | 빈 슬롯 클릭 → 예약 생성 (중복 검증 포함) |
| 4 | 예약 상세 · 취소 |
| 5 | 실시간 갱신, 반응형/UI 다듬기 |

---

## 10. 향후 고려사항
- 사용자 인증 및 권한(본인 예약만 수정/취소).
- 반복 예약, 회의실 정기 점검 시간 블록.
- 사용률 통계(부서별 · 회의실별).
